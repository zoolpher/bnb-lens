#include <iostream>

int main(int argc, char* argv[]) {
    std::cout << "Starting bnb-lens data capture..." << std::endl;
    
    // We will eventually add command-line argument parsing here
    // to read things like "--venue spot" and "--symbols BTCUSDT"
    
    return 0;
}
